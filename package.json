{
    "private": true,
    "scripts": {
        "dev": "vite",
        "build": "vite build",

        "serve": "bash ./vendor/bin/sail up",
        "migrate": "bash ./vendor/bin/sail artisan migrate",
        "deploy": "bash ./deploy.sh"
    },
    "devDependencies": {
        "axios": "^1.1.2",
        "laravel-vite-plugin": "^0.7.2",
        "vite": "^4.0.0"
    }
}
